import type { SDKAdapterInterface } from '@cloudbase/adapter-interface';
import { isMatch } from '../storage';
export declare class WxMpWebSocket {
    constructor(url: string, options?: object);
}
declare function genAdapterAsync(): Promise<SDKAdapterInterface>;
declare const adapter: {
    genAdapterAsync: typeof genAdapterAsync;
    genAdapter: any;
    isMatch: typeof isMatch;
    runtime: string;
};
export default adapter;
