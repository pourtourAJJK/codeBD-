export interface IDealCacheParams {
    getTcbInstance: () => Promise<{
        auth: any;
    }>;
    key: string;
    cacheKey?: string;
    value?: string;
    type?: 'get' | 'set' | 'remove';
}
/**
 * 该函数用于处理 signIn 和 getUserInfo 的缓存
 * 用户访问页面相关信息采用swr机制，进入页面优先使用缓存数据，后更新缓存，对于同一envId、clientId、userType、phone采用覆盖更新
 * @param param0
 * @returns
 */
export declare function dealCache({ key, getTcbInstance, value, type }: IDealCacheParams): Promise<any>;
/**
 * 解析weda的accessToken
 * @param wedaAccessToken
 * @returns
 */
export declare const parseWedaAccessToken: (wedaAccessToken: string) => any;
