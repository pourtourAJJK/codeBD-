export declare class LRUCache {
    #private;
    calculatedSize: number;
    constructor({ map, size, sizeCalculation, onInit, }: {
        map?: Map<string, {
            node: Node;
            value: any;
        }>;
        size: number;
        sizeCalculation?: (value: any, key: string) => number;
        onInit?: (cache: LRUCache) => void;
    });
    list(): {
        key: string;
        value: any;
    }[];
    get(key: string, options?: any): any;
    set(key: string, value: any, options?: any): void;
    delete(key: string): void;
}
declare class Node {
    data: {
        key: string;
        size: number;
    };
    pre: Node | null;
    next: Node | null;
    constructor(key: string, size: number);
}
export {};
