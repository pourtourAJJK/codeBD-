
import * as user from '../../../src/mp/auth/index';
describe('test src/mp/auth/index.ts currentUser', () => {
    it('test currentUser', () => {
        const res = user.auth.currentUser;
        expect(res).toMatchObject({
            type: ''
        });
    });
});