// 函数公式描述
import time from './time/declare';
import calculation from './calculation/declare';
import logic from './logic/declare';
import text from './text/declare';
const calculationData = {
    id: 'calculation',
    name: '计算',
    type: 'category',
    children: Object.values(calculation).sort((a, b) => a.Zindex - b.Zindex),
};
const logicData = {
    id: 'logic',
    name: '逻辑',
    type: 'category',
    children: Object.values(logic).sort((a, b) => a.Zindex - b.Zindex),
};
const timeData = {
    id: 'time',
    name: '日期时间',
    type: 'category',
    children: Object.values(time).sort((a, b) => a.Zindex - b.Zindex),
};
const textData = {
    id: 'text',
    name: '文本',
    type: 'category',
    children: Object.values(text).sort((a, b) => a.Zindex - b.Zindex),
};
export const formulaData = [calculationData, logicData, timeData, textData];
export default { formulaData };
