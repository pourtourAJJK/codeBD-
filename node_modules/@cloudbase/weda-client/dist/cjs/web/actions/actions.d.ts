import { IActionsAPI, IRouterAPI } from '../../types/web';
import { IWebAppAPI } from '../app';
/**
 * 将sdk挂在到app对象上，actions 分app 和 wx 挂载app 小程序的api
 * @param sdks
 * @returns
 */
export declare function createWebActionsAPI(app: Omit<IWebAppAPI, 'utils' | keyof IActionsAPI | keyof IRouterAPI>, sdks: any, options?: {
    processAction?: (name: any, action: any) => any;
}): IActionsAPI;
