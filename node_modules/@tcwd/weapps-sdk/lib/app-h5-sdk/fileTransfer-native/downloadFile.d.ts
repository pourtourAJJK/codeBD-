export default downloadFile;
declare function downloadFile({ url, filePath, header, timeout, success, fail, complete }: {
    url: string;
    header?: any;
    timeout?: any;
    filePath?: string;
    success?: Function;
    fail?: Function;
    complete?: Function;
}): any;
